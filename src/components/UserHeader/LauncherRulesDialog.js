import React from 'react';

const LauncherRulesDialog = ({ onClick }) => (
  <div id='helpIcon' onClick={onClick}>
    <i className="fas fa-question-circle">
    </i>
  </div>
)

export default LauncherRulesDialog;
